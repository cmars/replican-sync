
package track

import (

)

func Run() {
	for {
		select {
//		case fsUpdate := <-fsWatcher.Updates:
			// start settling timer on this fsnode in the tree
//		case treeReq := <-treeRequests:
			// respond with tree or tell them we're busy?
//		case updateSettled := <-updateFilter:
			// launch an index of the updated fsnode
//		case updateIndexed := <-indexResp:
			// update my tree, recalc strong at root
		}
	}
}

